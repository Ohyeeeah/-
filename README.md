# 培根斯的一天

一款本地优先的响应式每日计划 Web App。当前版本包含完整时间线、快速添加、完成进度、日期切换、移动到明天、逾期自动顺延、深浅主题与 JSON 备份。

## 本地运行

```bash
npm install
npm run dev
```

生产构建：

```bash
npm run build
```

数据保存在当前浏览器的 `localStorage` 中，不会上传到服务器。右上角按钮可导出或导入 JSON 备份。
