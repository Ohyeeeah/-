# Design

<!-- impeccable:design-schema 1 -->

## Direction

“鲜活的日程纸”：沿用纵向时间线的清晰秩序，以更鲜明但不刺眼的治愈色块标记一天的节奏。首屏必须同时看见当天心境、完成度和实际任务，而不是抽象仪表盘。

## Palette

- Paper `#FFFDF9`：主背景。
- Ink `#352F2D`：主文字与深色行动按钮。
- 浅色默认采用参考图的三色简约系统：淡蓝 `#A5D6DB`、暖米 `#F9E9D9`、浅绿 `#E8E8C6`。深浅变体只能服务对比和状态，不引入第四个色相；原珊瑚色角色在默认模式中映射到蓝色家族。
- 浅色“多彩模式”由顶栏「色彩」按钮开启：使用更清醒的青蓝 `#74DCE6`、暖杏 `#FFD29F`、活力黄绿 `#DFEC72` 与珊瑚红 `#FF5A43`，任务色块饱和度高于此前版本，但背景和文字仍维持克制。
- Dark：参考夜间公路照片建立与亮色完全不同的“夜路光轨”系统。界面骨架使用近黑青 `#070B0C` 与深青黑表面 `#111819`；任务色块只在电光青 `#20B7C3` 和道路反光黄绿 `#B7C94E` 两组主色之间交替，清除原有紫色。任务条采用保留辨识色的半透明磨砂表面，以明显的背景模糊让穿过的星线柔化散开；文字使用冰白与浅青灰。冰白 `#E6FFF9` 用于高优先级文字和少量光线，不再勾勒中心事项；车灯琥珀 `#F2C85C` 只用于日期、当前节点和少量提示。页面正文使用冷白与低饱和灰青。暗色主题维持少色、高对比，让黑色占据最大面积，亮色集中在任务和行动状态。背景另有一层稀疏矢量星线：以页面下部中央为消失点，低透明度的青色为主，冰白和黄绿为辅，不设置额外信标图形，营造荒凉、神秘的宇宙光轨感；不使用图片纹理或大面积发光。
- 不使用浑浊渐变；配色以纯色与低比例混色为主。

## Typography

问候标题使用 Dela Gothic One，以厚实、俏皮且稳定的展示黑体形成首屏记忆点；正文、界面数字与任务标题使用 Cascadia Next SC。标题的厚度由宽屏留白、舒展行宽和克制字距平衡，正文使用正常字重与舒展行距，数字统一使用 tabular numerals。两款字体均随项目附带 SIL Open Font License 1.1，可公开部署。

## Layout

桌面日程为覆盖宽屏画布的单视口双栏结构：页面本身不产生纵向滚动，左侧以稳定的窄栏承载日期、问候、行动提示、按日期保存的自由便笺与进度，右侧时间线伸展到剩余画布并独立滚动，不再受固定最大宽度限制；问候保持鲜明但不独占整列高度。任务条采用紧凑垂直密度，优先压缩空白而保留 44px 操作目标，让视口同时露出更多时间节点。日期箭头只在“昨天—今天—明天”三天窗口内移动，到达边界即停用。更早内容自动归入独立历史页，入口放在顶栏工具区，与日期导航清晰分工；历史记录作为长内容页面保留正常纵向滚动，并按日期折叠，只读呈现便笺、完成度和事项。移动端按相同阅读顺序折为单栏，时间线采用接近视口边缘的全宽舞台，时间与节点压缩为窄轨道，让任务卡占据主要宽度；内部焦点锚点固定在舞台上方但不绘制指针，首项紧跟进度出现，供末项经过锚点的额外滚动余量只放在列表末尾。桌面仍在中央计算视觉层级，但不绘制焦点线。时间线舞台不添加白色覆盖渐隐，任务的距离渐隐直接融入页面背景。快速输入固定在视口底部、接近两侧安全边距，并预留底部安全区域。

## Components and States

- 任务面使用 16px 圆角和单一柔和投影；浅色任务不叠加边框，暗色半透明磨砂任务以 24px 背景模糊和 1px 同色低透明边界维持玻璃层次。速度线画布置于日程主画布内部、任务卡之下，使卡片能实际采样并柔化穿过的光线。纵向密度保持紧凑，让桌面右栏在一屏内能形成清晰的多事项上下文，同时保留 44px 操作热区。
- 小型工具使用圆形或紧凑控制；胶囊只用于日期/时间等短信息。底部快速输入栏使用可透出下方任务的半透明表面与轻度背景模糊，文字和主要按钮保持实色对比。
- 完成态同时使用勾选、删除线与透明度，不只依赖颜色。
- 自由便笺按日期保存，直接输入即持久化，并随任务一起进入 JSON 备份；旧版任务数组备份仍可导入。
- 必须保留空状态、键盘焦点、深色主题和减少动态效果。

## Motion

暗色背景光条采用固定种子生成的不规则星流：约八成分布在画面边缘，中心保持稀疏；青、冰白、黄绿与极少量琥珀按权重出现，并分别随机化长度、粗细、亮度、漂移距离、周期和相位。每条光线在循环首尾隐去，避免整组同步和接缝跳变。

时间线采用自由惯性而非项目吸附。滚轮输入直接驱动可中断速度，新的反向输入在下一帧立即接管方向；松手后速度单调衰减并停在用户实际滚到的位置，不启动第二段居中或回弹动画。触控和键盘保留浏览器原生停止位置。

任务首次进入时依次轻落并从模糊转清晰；空状态轨道缓慢旋转。时间线初次打开时根据当前时间把下一项放到内部焦点锚点；此后任务只依据与该锚点的实际像素距离逐帧缩小、渐隐并降低饱和度，不绘制指针，也不给最近事项附加强调描边。进度曲线保持扁平单色主线，仅在其下叠一层同色、低强度的柔光，不使用亮色灯芯、多重光圈或立体管状高光；光色跟随主题主色，完成度变化以约 960ms 的连续弯曲推进表现，快速重复勾选时从当前进度无缝改向新目标。暗色背景的星线以 14–22 秒的错峰周期缓慢向外流动，星点只做低振幅呼吸，循环无明显接缝且不抢夺任务层级。主题切换采用约 760ms 的离散状态编排：主题属性在浏览器绘制前同步，页面背景先变化，卡片与输入表面随后跟进，主文字延迟翻色，次要文字、轨道和图标最后抵达；过渡规则常驻，不在结束时移除，因此不会产生尾帧闪色。主题图标同时完成一次短促转动。所有动态尊重 `prefers-reduced-motion`，减少动态时立即完成颜色与焦点切换，星线保留为静态背景，并保留静态柔光作为完成度反馈。

## Voice

俏皮、积极、短促、有动作感，像会轻推一把的队友；使用“开局、推进、拿下、归队、接力”等词，把反馈变成即时的小胜利。避免过度松弛的安抚语气，也避免训诫、羞辱、连续打卡压力和夸张成功承诺。
